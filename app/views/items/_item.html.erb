<div>
	<h2><%= link_to(item.title, item) %></h2>
	<p>
		<%=raw item.description %>
	</p>
	<span>Published: 
		<% if item.published != nil %>
	 	<%= item.published.split('.')[0] %>
	 	<% end %>
	 </span>
	<div>Read? <%= item.read? %></div>
</div>


