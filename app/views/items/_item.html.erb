
<% if item.read? %>
<div class="closed-item read", id="item-<%= item.id%>">
<% else %>
<div class="closed-item unread", id="item-<%= item.id%>">
<% end %>

	<%= link_to(item.title, item, remote: true) %>
</div>

